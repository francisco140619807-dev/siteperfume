import { useEffect, useRef, useState } from "react";
const START=14850, END=15000, DURATION=1500;
export default function MemberCounter(){const ref=useRef<HTMLSpanElement|null>(null);const [value,setValue]=useState(START);const animated=useRef(false);useEffect(()=>{const el=ref.current;if(!el)return;const run=()=>{if(animated.current)return;animated.current=true;let current=START;const stepTime=DURATION/((END-START)/3);const timer=window.setInterval(()=>{current+=3;if(current>=END){current=END;window.clearInterval(timer);setValue(END)}else setValue(current)},stepTime)};const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){run();observer.disconnect()}}),{threshold:.5});observer.observe(el);return()=>observer.disconnect()},[]);return <span id="member-counter" ref={ref}>{value>=END?"+15.000":`+${value.toLocaleString("pt-BR")}`}</span>}
