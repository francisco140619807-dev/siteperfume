import { useState, type MouseEvent, type ReactNode } from "react";
import WhatsAppIcon from "./WhatsAppIcon";
import { openWhatsAppGroup, WHATSAPP_GROUP_URL } from "../lib/whatsapp";
import { trackWhatsAppLead } from "../lib/meta";
type Props={className:string;label:ReactNode;iconClassName?:string};
export default function WhatsAppButton({className,label,iconClassName="w-7 h-7"}:Props){const [stuck,setStuck]=useState(false);const handleClick=(event:MouseEvent<HTMLAnchorElement>)=>{if(event.metaKey||event.ctrlKey||event.shiftKey||event.button!==0)return;event.preventDefault();trackWhatsAppLead();openWhatsAppGroup(()=>setStuck(true));};return <><a href={WHATSAPP_GROUP_URL} onClick={handleClick} target="_blank" rel="noopener noreferrer" className={className}><WhatsAppIcon className={iconClassName}/><span>{label}</span></a>{stuck&&<div className="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-black/85"><div className="glass-card w-full max-w-sm rounded-2xl p-6 text-center"><div className="text-4xl mb-3">📲</div><h3 className="font-heading text-xl font-black text-white mb-2">Falta só um passo!</h3><p className="text-sm text-gray-300 mb-4">Você está no navegador interno do app. Abra no navegador externo para entrar no grupo.</p><button type="button" onClick={()=>setStuck(false)} className="text-sm font-semibold text-gray-400 hover:text-white py-2">Fechar</button></div></div>}</>}
